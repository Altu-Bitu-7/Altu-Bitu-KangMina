// 물병
#include <iostream>
using namespace std;

// 처음에 모든 물병에는 물이 1리터씩 들어있다.
// 먼저 같은 양의 물이 들어있는 물병 두 개를 고른다. 그 다음에 한 개의 물병에 다른 한 쪽에 있는 물을 모두 붓는다. 
// 이 방법을 필요한 만큼 계속 한다.
// n개로 k개를 넘지않는 비어있지 않은 물병을 만드는 것

int main(){
    int n, k; // n: 가진 물병의 개수, k: 한번에 옮길 수 있는 물병의 개수

    // 입력
    cin >> n >> k;

    // 연산

    // 출력: 상점에서 사야하는 물병의 최솟값. (정답 없으면 -1 출력)
    cout << answ;
    
    retrun 0;
}