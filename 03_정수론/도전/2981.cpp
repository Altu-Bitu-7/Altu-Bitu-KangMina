// 검문
#include <iostream>
#include <vector>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);  cout.tie(NULL);

    int n;  // n: input의 개수(2이상 100이하)
    cin >> n;

    for (int i = 0; i < n; i++){
        int input;  // input: 종이에 적은 수
        cin >> m;
    }

    return 0;
}